$kind: http-request
name: Assign Request
method: PATCH
url: '{{baseUrl}}/api/requests/{{requestId}}/assign'
order: 6000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "assignedTo": "{{staffId}}"
    }
